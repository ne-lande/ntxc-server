# Generated by setupmc.com on 2025-12-10T00:15:28.241Z

services:
  minecraft:
    image: itzg/minecraft-server:latest
    tty: true
    stdin_open: true
    restart: unless-stopped
    ports:
      - "25565:25565"
    environment:
      EULA: "TRUE"
      TYPE: "FABRIC"
      VERSION: "1.21.11"
      MEMORY: "12288M"
      MAX_PLAYERS: "50"
      MOTD: "Hewwo!!! Welcome to the server!!!"
      ONLINE_MODE: "false"
      USE_MEOWICE_FLAGS: "true"
      TZ: "Europe/Moscow"
      DIFFICULTY: "2"
      FORCE_GAMEMODE: "true"
      SPAWN_PROTECTION: "0"
      MAX_WORLD_SIZE: "1000000"
      REGION_FILE_COMPRESSION: "lz4"
      PREVENT_PROXY_CONNECTIONS: "true"
      HIDE_ONLINE_PLAYERS: "true"
      ENFORCE_SECURE_PROFILE: "false"
      PLAYER_IDLE_TIMEOUT: "10"
      UID: "1001"
      GID: "1001"
    volumes:
      - "./data:/data"

  bluemap:
    image: ghcr.io/bluemap-minecraft/bluemap:latest
    restart: always
    command: -r -u -w
    ports:
      - "8100:8100"
    volumes:
      - "./config:/app/config"
      - "./world:/app/world"
      - "./world_nether:/app/world_nether"
      - "./world_the_end:/app/world_the_end"
      - "./data:/app/data"
      - "./web:/app/web"

  nginx:
    image: nginx:latest
    restart: always
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - "./nginx/conf.d:/etc/nginx/conf.d"
      - "./nginx/html:/usr/share/nginx/html"
      - "./nginx/certs:/etc/nginx/certs"
    depends_on:
      - minecraft
      - bluemap
